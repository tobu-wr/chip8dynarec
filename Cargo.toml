[package]
name = "chip8dynarec"
version = "1.0.0"
authors = ["Wilfried Rabouin"]

[dependencies]
rand = "0.3"
sdl2 = "0.30"
memmap = "0.5.2"

[features]
# no features by default
default = []
debugger = []
interpreter = []

[profile.release]
lto = true
